# Built native libraries (regenerated by build-android.sh)
jniLibs/
build/
# Keep the gomobile-generated Java bindings JAR (11KB stub classes).
# This is needed for Kotlin compilation even when the Go build hasn't run.
!build/rcbridge-bindings.jar
# Go build cache
go/vendor/
